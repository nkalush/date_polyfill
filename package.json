{
  "name": "date_polyfill",
  "version": "0.0.1",
  "description": "drop in polyfill for the inputs with a type of date that tries to mimic chrome's implementation.",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-jshint": "^0.11.0",
    "grunt-contrib-uglify": "^0.7.0",
    "grunt-contrib-watch": "^0.6.1",
    "grunt-jslint": "^1.1.12"
  }
}
